import { fetchTables } from './actions/index';
import { FETCH_TABLES } from './actions/types';
import { $action } from 'rxjs';

// const fetchTablesEpic = action$ =>
//     action$.ofType(FETCH_TABLES)
